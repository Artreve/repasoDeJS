//query selector all: retorna todos los elementos que cumpla con con la condiciond el selector
const card = document.querySelectorAll('.card')
console.log(card)

//si un elemento no existe entonces nos retorna un array vacio